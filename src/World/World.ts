import { Camera, Scene, WebGLRenderer } from "three";


export class World {

    constructor(container: HTMLElement | null, camera: Camera, scene: Scene, renderer: WebGLRenderer) {
        // todo https://discoverthreejs.com/zh/book/first-steps/animation-loop/#the-update-loop
    }

    render() { }
}
